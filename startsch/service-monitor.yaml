apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: startsch
  namespace: monitoring
  labels:
    release: kube-prom-stack
spec:
  namespaceSelector:
    matchNames:
      - startsch
  endpoints:
    - port: http
      path: /metrics
